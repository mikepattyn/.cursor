# Recipe-only: Flutter calculator

Not a full Flutter project (no `android/`, `ios/`, or generated bindings). Copy into an app created with `flutter create`, then:

```bash
flutter test
flutter run
```

Keep this leaf outside the pnpm graph. Do not add a `package.json` that pnpm would install.
