# Recipe-only: Rails API values controller

Not a full Rails app. Copy into an API generated with `rails new example-api --api`.

```bash
bundle exec ruby -Itest test/values_controller_test.rb
```

SQLite file at `SQLITE_PATH`. Do not wrap this as an npm package.
