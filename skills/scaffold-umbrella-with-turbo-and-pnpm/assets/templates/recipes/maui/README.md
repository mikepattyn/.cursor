# Recipe-only: .NET MAUI calculator page

Not a full MAUI solution (no Platforms folders). Drop `CalculatorPage.xaml.cs` into an app created with `dotnet new maui`. Signing and provisioning stay outside the repo.

```bash
dotnet test
```

Do not add a `package.json` for this project.
